package db.dao;

import db.bean.CallEntry;

public class CallEntryDAO {
   // private final SessionFactory sessionFactory = new HibernateUtil(CallEntry.class).getSession();

    public void save(CallEntry callEntry) {
//        sessionFactory.openSession();
//        sessionFactory.getCurrentSession().beginTransaction();
//        sessionFactory.getCurrentSession().save(callEntry);
//        sessionFactory.getCurrentSession().close();
//        sessionFactory.close();
    }
}
